import { motion } from "framer-motion";
export default function Profile_Contact() {
  return (
    <div className="min-h flex flex-col z-50">
      <div className="flex flex-col px-2 code-saver text-[12px] leading-3.5 font-medium">
        <motion.pre 
        initial={{ clipPath: "inset(0 100% 0 0)" }}
        animate={{ clipPath: "inset(0 0% 0 0)" }}
        transition={{ duration: 2.1, ease: "easeOut" ,delay: 1}}
        className="text-gray-400">
          &lt;!-- start .scope-contact --&gt;
        </motion.pre>
  
        <motion.pre 
        initial={{ clipPath: "inset(0 100% 0 0)" }}
        animate={{ clipPath: "inset(0 0% 0 0)" }}
        transition={{ duration: 2.1, ease: "easeOut",delay: 0.7 }}
        className="text-blue-300">
          &lt;section <span className="text-blue-200">class</span>=
          "<span className="text-orange-300">scope-contact</span>"&gt;
        </motion.pre>
  
        <motion.pre 
        initial={{ clipPath: "inset(0 100% 0 0)" }}
        animate={{ clipPath: "inset(0 0% 0 0)" }}
        transition={{ duration: 2.1, ease: "easeOut",delay: 1 }}
        className="pl-4 text-blue-300">
          &lt;h3&gt;
        </motion.pre>
  
        <motion.pre 
        initial={{ clipPath: "inset(0 100% 0 0)" }}
        animate={{ clipPath: "inset(0 0% 0 0)" }}
        transition={{ duration: 2.1, ease: "easeOut" }}
        className="pl-8 text-white">GET IN TOUCH
        </motion.pre>
  
        <motion.pre 
        initial={{ clipPath: "inset(0 100% 0 0)" }}
        animate={{ clipPath: "inset(0 0% 0 0)" }}
        transition={{ duration: 2.1, ease: "easeOut",delay: 1 }}
        className="pl-4 text-blue-300">
          &lt;h3&gt;
        </motion.pre>
  
        <motion.pre 
        initial={{ clipPath: "inset(0 100% 0 0)" }}
        animate={{ clipPath: "inset(0 0% 0 0)" }}
        transition={{ duration: 2.1, ease: "easeOut",delay: 0.8 }}
        className="pl-4 text-blue-300">
          &lt;p&gt;
        </motion.pre>
  
        <motion.pre 
        initial={{ clipPath: "inset(0 100% 0 0)" }}
        animate={{ clipPath: "inset(0 0% 0 0)" }}
        transition={{ duration: 1.6, ease: "easeOut",delay:0.3 }}
        className="pl-8 mr-6 text-gray-300 text-[12px] leading-4 font-codesaver whitespace-pre-wrap ">
          {` I'm currently open to freelance collaborations 
with agencies and designers. If you're looking for a
reliable developer to implement your 
digital projects, let’s connect.`}
        </motion.pre>
  
        <motion.pre 
        initial={{ clipPath: "inset(0 100% 0 0)" }}
        animate={{ clipPath: "inset(0 0% 0 0)" }}
        transition={{ duration: 2.1, ease: "easeOut",delay: 0.8 }}
        className="pl-4 text-blue-300">
          &lt;/p&gt;
        </motion.pre>
  
        <motion.pre 
        initial={{ clipPath: "inset(0 100% 0 0)" }}
        animate={{ clipPath: "inset(0 0% 0 0)" }}
        transition={{ duration: 2.1, ease: "easeOut",delay: 0.8 }}
        className="pl-4 text-blue-300">
          &lt;p&gt;
        </motion.pre>
  
        <motion.pre 
        initial={{ clipPath: "inset(0 100% 0 0)" }}
        animate={{ clipPath: "inset(0 0% 0 0)" }}
        transition={{ duration: 1.6, ease: "easeOut",delay:0.3 }}
        className="pl-8 mr-6 text-gray-300 text-[12px] leading-4 font-codesaver whitespace-pre-wrap ">
          {`Please feel free to shoot me 
an email to organise a meeting — 
I’d love to hear what you’re working on.`}
        </motion.pre>
  
        <motion.pre 
        initial={{ clipPath: "inset(0 100% 0 0)" }}
        animate={{ clipPath: "inset(0 0% 0 0)" }}
        transition={{ duration: 2.1, ease: "easeOut",delay: 0.8 }}
        className="pl-4 text-blue-300">
          &lt;/p&gt;
        </motion.pre>
  
        <motion.pre 
        initial={{ clipPath: "inset(0 100% 0 0)" }}
        animate={{ clipPath: "inset(0 0% 0 0)" }}
        transition={{ duration: 1.6, ease: "easeOut",delay:0.3 }}
        className="pl-4 text-blue-300">
          &lt;a href = "aruljayarajj826@gmail.com" &gt;
        </motion.pre>
    
        <motion.pre 
        initial={{ clipPath: "inset(0 100% 0 0)" }}
        animate={{ clipPath: "inset(0 0% 0 0)" }}
        transition={{ duration: 2.1, ease: "easeOut",delay: 0.7 }}
        className="pl-8 mr-6 text-gray-300 text-[12px] leading-4 font-codesaver whitespace-pre-wrap ">
          {`Email Me`}
        </motion.pre>       
  
        <motion.pre 
        initial={{ clipPath: "inset(0 100% 0 0)" }}
        animate={{ clipPath: "inset(0 0% 0 0)" }}
        transition={{ duration: 2.1, ease: "easeOut",delay: 0.8 }}
        className="pl-4 text-blue-300">
          &lt;/a&gt;
        </motion.pre>
  
        <motion.pre 
        initial={{ clipPath: "inset(0 100% 0 0)" }}
        animate={{ clipPath: "inset(0 0% 0 0)" }}
        transition={{ duration: 2.1, ease: "easeOut",delay: 0.7 }}
        className="text-blue-300">
          &lt;/section&gt;
        </motion.pre>
        </div>
    </div>
  );
}
